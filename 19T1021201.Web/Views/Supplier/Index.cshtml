@model IEnumerable<_19T1021201.DomainModels.Supplier>
@{
    ViewBag.Title = "Nhà cung cấp";
}

<div class="box box-primary">
    <div class="box-body">

        <form>
            <div class="input-group">
                <input type="text" class="form-control" placeholder="Search" value="@ViewBag.SearchValue">
                <div class="input-group-btn">
                    <button class="btn btn-success" type="submit">
                        <i class="glyphicon glyphicon-search"></i>
                    </button>
                </div>
                <div class="input-group-btn">
                    <a href="~/Supplier/Create" class="btn btn-primary" style="margin-left: 5px;">
                        <i class="glyphicon glyphicon-plus"></i>
                        bổ sung
                    </a>
                </div>
            </div>

        </form>


        <p>Có <b>@ViewBag.RowCount</b> nhà cung cấp trong tổng số <b>@ViewBag.PageCount</b> trang</p>

        <table class="table table-bordered table-hover table-striped">
            <thead>
                <tr class="bg-primary">
                    <th>
                        Tên nhà cung cấp
                    </th>
                    <th>
                        Tên giao dịch
                    </th>
                    <th>
                        Địa chỉ
                    </th>
                    <th>
                        Điện thoại

                    </th>
                    <th>
                        Quốc gia

                    </th>
                    <th>
                        Thành phố
                    </th>
                    <th>
                        Mã bưu chính
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @item.SupplierName
                        </td>
                        <td>
                            @item.ContactName
                        </td>
                        <td>
                            @item.Address
                        </td>
                        <td>
                            @item.Phone
                        </td>
                        <td>
                            @item.Country
                        </td>
                        <td>
                            @item.City
                        </td>
                        <td>
                            @item.PostalCode
                        </td>
                        <td class="text-center" style="width: 90px">
                            <a href="~/Supplier/Edit/@item.SupplierID" class="btn btn-xs btn-info">
                                <i class="glyphicon glyphicon-edit"></i>
                            </a>
                            <a href="~/Supplier/Delete/@item.SupplierID" class="btn btn-xs btn-danger">
                                <i class="glyphicon glyphicon-trash"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <div class="text-center">
            <ul class="pagination">
                @for (int i = 1; i <= ViewBag.PageCount; i++)
                {
                    if (i == ViewBag.Page)
                    {
                        <li class="active"><a href="#">@i</a></li>
                    }
                    else
                    {
                        <li><a href="~/Supplier?page=@i&searchvalue=@ViewBag.SearchValue">@i</a></li>
                    }
                }
            </ul>
        </div>
    </div>
</div>


